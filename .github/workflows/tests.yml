name: Tests

on:
  pull_request:
  push:
    branches: [ "**" ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Placeholder tests
        run: |
          echo "Hook real test runner when available (e.g., cargo test, pytest, go test)"