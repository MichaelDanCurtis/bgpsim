name: Lint

on:
  pull_request:
  push:
    branches: [ "**" ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Placeholder lint
        run: |
          echo "Enable real linters per language in future PRs"